# String1 problems
# hello_name
def hello_name(name):
  return ("Hello " + name + "!")

# non_start
def non_start(a, b):
  return a[1:] + b[1:]

# first_half
def first_half(str):
  return str[0:(len(str))/2]

